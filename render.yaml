services:
  - type: web
    name: financial-risk-assessor
    env: docker
    plan: free
    region: oregon # or your preferred region
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: true
